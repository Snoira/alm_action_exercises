name: test python hello world
on: 
    push:
        branches:
            - main
jobs: 
    python-hello-world:
        runs-on: ubuntu-latest
        steps: 
            - name: Checkout repository
              uses: actions/checkout@v2

            - name: hello.py
              run: python hello.py
